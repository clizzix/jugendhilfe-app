# -----------------------------------------------------------------
# 1. Node und Abhängigkeiten
# -----------------------------------------------------------------
node_modules/
shared/node_modules/
/npm-debug.log
.DS_Store # Mac OS spezifisch

# -----------------------------------------------------------------
# 2. Sensible Daten und Konfiguration (MUSS IGNORIERT WERDEN)
# -----------------------------------------------------------------
# ----------------------------------------------------------------------
# Expo Build Artifacts & Credentials (NIE committen!)
# ----------------------------------------------------------------------
*.apk
*.aab
*.ipa
android/
ios/

.env 
backend/.env
# Enthält MONGO_URI, JWT_SECRET und später API-Schlüssel für DeepL/OCR!
google-vision-key.json
# -----------------------------------------------------------------
# 3. Temporäre Dateien und Uploads
# -----------------------------------------------------------------
# Temporärer Speicherort für Multer-Uploads (aus reportRoutes.js)
backend/routes/uploads
web/dist/
/build
mobile/android/
mobile/ios/

.vscode/
npm-debug.log*